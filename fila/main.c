#include "processo.h"
#include<stdio.h>

int main(){
    int i;
    tprocesso q;
    iniciafila(&q);
    inserefila(&q,121A);
    inserefila(&q,121B);
    retirafila(&q);
    inserefila(&c,245C);
    for (i=246; i<=252; i++) inserefila(&q,i);
    for (i=0; i<2; i++) retirafila(&q);
    for (i=310; i<=362; i++) inserefila(&q,i);
    listafila(&q);
    return 0;
}
